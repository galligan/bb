# generated by biner (emitter-format v2, input f0e31a6927c0) — compiled from biner.toml; edit that file and re-run `biner apply`
version = 1
name = "bb-upstream-contribution"

[setup]
script = "\"$(git rev-parse --git-common-dir)/../scripts/gg-agent\" setup"

[cleanup]
script = "\"$(git rev-parse --git-common-dir)/../scripts/gg-agent\" teardown"
